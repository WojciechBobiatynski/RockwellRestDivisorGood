<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
       xmlns:beans="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans    http://www.springframework.org/schema/beans/spring-beans-4.1.xsd
                           http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.2.xsd">

    <global-method-security secured-annotations="enabled" />

    <http pattern="/ti/**" use-expressions="true">
        <intercept-url pattern="/resource/**" access="permitAll" />
        <form-login login-page="/ti/login"

                    authentication-failure-url="/ti/login?error"
                    default-target-url="/"
                    password-parameter="password"
                    username-parameter="username"/>
        <logout logout-success-url="/ti/login"
                logout-url="/logout"
                invalidate-session="true"/>
        <intercept-url pattern="/ti/login" access="permitAll" />
        <intercept-url pattern="/logout" access="permitAll" />
        <intercept-url pattern="/**" access="isAuthenticated()" />
        <http-basic/>
    </http>

    <http use-expressions="true">
        <intercept-url pattern="/resource/**" access="permitAll" />
        <form-login login-page="/login"
                             authentication-failure-url="/login?error"
                             default-target-url="/"
                             password-parameter="password"
                             username-parameter="username"/>
        <logout logout-success-url="/"
                         logout-url="/logout"
                         invalidate-session="true"/>
        <intercept-url pattern="/login" access="permitAll" />
        <intercept-url pattern="/logout" access="permitAll" />
        <intercept-url pattern="/**" access="isAuthenticated()" />
        <http-basic/>
    </http>




    <authentication-manager>
        <authentication-provider ref="gryfAuthenticationProvider"/>
        <authentication-provider ref="trainingInstitutionAuthenticationProvider" />
    </authentication-manager>

    <beans:bean id="gryfAuthenticationProvider" class="pl.sodexo.it.gryf.web.security.GryfAuthenticationProvider" />
    <beans:bean id="trainingInstitutionAuthenticationProvider" class="pl.sodexo.it.gryf.web.security.traininginstitutions.TrainingInstitutionUserAuthenticationProvider" />

</beans:beans>